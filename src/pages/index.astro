---
title: "Interest Form"
description: "Sign up for coaching updates"
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Coach Audie | Interest Form</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: sans-serif;
        background-color: #f7f7f7;
        color: #333;
      }

      main {
        max-width: 600px;
        margin: 2rem auto;
        background-color: #fff;
        padding: 2rem;
        border-radius: 6px;
      }

      header {
        text-align: center;
        margin-bottom: 2rem;
      }

      label {
        display: block;
        margin: 1rem 0 0.25rem;
        font-weight: bold;
      }

      input,
      textarea {
        width: 100%;
        padding: 0.5rem;
        border: 1px solid #ccc;
        border-radius: 4px;
      }

      button {
        margin-top: 1.5rem;
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 4px;
        background-color: #0077ff;
        color: #fff;
        cursor: pointer;
      }

      button:hover {
        background-color: #005ecc;
      }

      /* Honeypot container hidden visually; 
         also remove from tab order and screen readers if desired. */
      .honeypot {
        display: none;
      }
    </style>
  </head>
  <body>
    <main>
      <header>
        <h1>Interested in Coaching?</h1>
        <p>Fill out the form, and I’ll follow up via text.</p>
      </header>

      <form action="#" method="post" aria-label="Coaching interest form">
        <!-- Honeypot Field -->
        <div class="honeypot" aria-hidden="true">
          <label for="honeypot">Leave this field empty if you're human</label>
          <input type="text" id="honeypot" name="honeypot" tabindex="-1" autocomplete="off" />
        </div>
        
        <div>
          <label for="name">Name</label>
          <input type="text" id="name" name="name" required />
        </div>
        
        <div>
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required />
        </div>
        
        <div>
          <label for="phone">Phone (text only)</label>
          <input type="tel" id="phone" name="phone" required />
        </div>
        
        <div>
          <label for="description">Why do you want coaching?</label>
          <textarea id="description" name="description" rows="4" required></textarea>
        </div>
        
        <button type="submit">Submit</button>
      </form>
    </main>
  </body>
</html>